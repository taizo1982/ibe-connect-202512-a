---
import { Image } from 'astro:assets';
import Button from '../common/Button.astro';
import event from '../../data/event.json';
import heroBackground from '../../assets/images/back_img.png';
---

<section id="hero" class="relative min-h-screen flex items-center overflow-hidden pt-16">
  <!-- Background Image -->
  <div class="absolute inset-0 z-0">
    <Image
      src={heroBackground}
      alt=""
      class="w-full h-full object-cover"
      loading="eager"
    />
    <div class="absolute inset-0 bg-black/40"></div>
  </div>
  <div class="container mx-auto px-4 py-12 md:py-20 relative z-10">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Badge -->
      <div class="mb-6 fade-in-up">
        <span class="inline-block bg-white/90 text-primary-pink-dark px-5 py-2 rounded-full text-sm font-medium">
          {event.dateDisplay}開催
        </span>
      </div>

      <!-- Main Catchcopy -->
      <h1 class="font-display text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-bold text-white leading-tight mb-6 fade-in-up text-balance drop-shadow-lg">
        アプリでは分からない<span class="text-primary-pink-light">"人柄"</span>から、<br class="hidden sm:inline" />
        ちゃんと好きになれる出会いを。
      </h1>

      <!-- Subcopy -->
      <p class="text-base md:text-lg text-white/90 mb-8 max-w-2xl mx-auto fade-in-up drop-shadow">
        野洲駅近くの人気3店舗をはしごしながら、<br class="hidden md:inline" />
        30〜40代同士が自然体でじっくり話せる、大人のバル婚活イベントです。
      </p>

      <!-- Event Info Cards -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-3 md:gap-4 max-w-2xl mx-auto mb-10 fade-in-up">
        <div class="bg-white/80 backdrop-blur-sm rounded-xl p-3 md:p-4 shadow-sm">
          <div class="text-primary-pink text-2xl mb-1">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
            </svg>
          </div>
          <p class="text-xs text-gray-500">開催日</p>
          <p class="text-sm font-bold text-gray-800">2月28日（金）</p>
        </div>

        <div class="bg-white/80 backdrop-blur-sm rounded-xl p-3 md:p-4 shadow-sm">
          <div class="text-primary-blue text-2xl mb-1">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
          </div>
          <p class="text-xs text-gray-500">エリア</p>
          <p class="text-sm font-bold text-gray-800">JR野洲駅周辺</p>
        </div>

        <div class="bg-white/80 backdrop-blur-sm rounded-xl p-3 md:p-4 shadow-sm">
          <div class="text-accent-gold text-2xl mb-1">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <p class="text-xs text-gray-500">形式</p>
          <p class="text-sm font-bold text-gray-800">3店舗はしご</p>
        </div>

        <div class="bg-white/80 backdrop-blur-sm rounded-xl p-3 md:p-4 shadow-sm">
          <div class="text-primary-pink text-2xl mb-1">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
            </svg>
          </div>
          <p class="text-xs text-gray-500">対象</p>
          <p class="text-sm font-bold text-gray-800">30〜40代独身</p>
        </div>
      </div>

      <!-- Price -->
      <div class="flex flex-wrap justify-center gap-4 mb-8 fade-in-up">
        <span class="bg-white/90 text-primary-blue-dark px-5 py-2 rounded-full text-sm font-medium">
          男性 {event.price.male.toLocaleString()}円
        </span>
        <span class="bg-white/90 text-primary-pink-dark px-5 py-2 rounded-full text-sm font-medium">
          女性 {event.price.female.toLocaleString()}円
        </span>
      </div>

      <!-- CTA -->
      <div class="fade-in-up">
        <Button href={event.entryUrl} size="large" pulse={true}>
          このイベントに申し込む
        </Button>
        <p class="text-sm text-white/80 mt-4">
          ※定員になり次第締切／1人参加・婚活初心者歓迎
        </p>
      </div>
    </div>
  </div>

  <!-- Decorative elements -->
  <div class="absolute -bottom-32 -left-32 w-64 h-64 bg-primary-pink/10 rounded-full blur-3xl pointer-events-none"></div>
  <div class="absolute -top-32 -right-32 w-64 h-64 bg-primary-blue/10 rounded-full blur-3xl pointer-events-none"></div>
</section>

<style>
  .shadow-sm {
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
  }
</style>
