---
import { Image } from 'astro:assets';
import SectionTitle from '../common/SectionTitle.astro';
import event from '../../data/event.json';
import sparkJoyImg from '../../assets/images/spark-joy.webp';
import waenImg from '../../assets/images/waen.jpg';
import hanabiImg from '../../assets/images/hanabi.webp';
---

<section id="concept" class="section-padding bg-white">
  <div class="container mx-auto px-4">
    <div class="max-w-4xl mx-auto">
      <SectionTitle class="fade-in-up">
        最初から「会って話す」ことにフォーカスした、<br class="hidden md:inline" />大人のバル婚活
      </SectionTitle>

      <div class="text-center mb-10 fade-in-up">
        <p class="text-gray-600 text-lg leading-relaxed max-w-2xl mx-auto">
          このイベントは、マッチングアプリのように「メッセージを重ねてからやっと会う」のではなく、
          <span class="font-bold text-gray-800">はじめから"会って話すこと"にフォーカス</span>した出会いの場です。
        </p>
      </div>

      <!-- 3店舗のカード -->
      <div class="grid md:grid-cols-3 gap-6 mb-10 fade-in-up">
        <!-- 1軒目: SPARK JOY -->
        <a
          href={event.venues[0].url}
          target="_blank"
          rel="noopener noreferrer"
          class="group bg-white rounded-2xl overflow-hidden shadow-card hover:shadow-card-hover transition-shadow duration-300 stagger-item"
        >
          <div class="aspect-[4/3] overflow-hidden">
            <Image
              src={sparkJoyImg}
              alt={event.venues[0].name}
              class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
            />
          </div>
          <div class="p-5">
            <div class="flex items-center gap-2 mb-2">
              <span class="inline-block bg-primary-pink/10 text-primary-pink text-xs font-bold px-2 py-1 rounded-full">
                1軒目
              </span>
              <span class="text-xs text-gray-500">{event.venues[0].type}</span>
            </div>
            <h3 class="font-display font-bold text-gray-800 mb-1">{event.venues[0].name}</h3>
            <p class="text-sm text-gray-600">{event.venues[0].menu}</p>
          </div>
        </a>

        <!-- 2軒目: わえん食堂 -->
        <a
          href={event.venues[1].url}
          target="_blank"
          rel="noopener noreferrer"
          class="group bg-white rounded-2xl overflow-hidden shadow-card hover:shadow-card-hover transition-shadow duration-300 stagger-item"
        >
          <div class="aspect-[4/3] overflow-hidden">
            <Image
              src={waenImg}
              alt={event.venues[1].name}
              class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
            />
          </div>
          <div class="p-5">
            <div class="flex items-center gap-2 mb-2">
              <span class="inline-block bg-primary-blue/10 text-primary-blue text-xs font-bold px-2 py-1 rounded-full">
                2軒目
              </span>
              <span class="text-xs text-gray-500">{event.venues[1].type}</span>
            </div>
            <h3 class="font-display font-bold text-gray-800 mb-1">{event.venues[1].name}</h3>
            <p class="text-sm text-gray-600">{event.venues[1].menu}</p>
          </div>
        </a>

        <!-- 3軒目: 華火 HANABI -->
        <a
          href={event.venues[2].url}
          target="_blank"
          rel="noopener noreferrer"
          class="group bg-white rounded-2xl overflow-hidden shadow-card hover:shadow-card-hover transition-shadow duration-300 stagger-item"
        >
          <div class="aspect-[4/3] overflow-hidden">
            <Image
              src={hanabiImg}
              alt={event.venues[2].name}
              class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
            />
          </div>
          <div class="p-5">
            <div class="flex items-center gap-2 mb-2">
              <span class="inline-block bg-accent-gold/10 text-accent-gold text-xs font-bold px-2 py-1 rounded-full">
                3軒目
              </span>
              <span class="text-xs text-gray-500">{event.venues[2].type}</span>
            </div>
            <h3 class="font-display font-bold text-gray-800 mb-1">{event.venues[2].name}</h3>
            <p class="text-sm text-gray-600">{event.venues[2].menu}</p>
          </div>
        </a>
      </div>

      <div class="text-center fade-in-up">
        <p class="text-sm text-gray-500 mb-4">
          ※店舗を巡る順番はグループにより異なります
        </p>
        <p class="text-gray-600 leading-relaxed max-w-2xl mx-auto">
          お店の雰囲気が変わるたびに、会話の空気も自然と柔らかくなり、<br class="hidden md:inline" />
          <span class="font-medium text-gray-800">プロフィールでは分からない"人柄"に触れやすい時間</span>が流れていきます。
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .shadow-card {
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
  }
  .shadow-card-hover {
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
  }
</style>
